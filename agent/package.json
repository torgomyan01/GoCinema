{
  "name": "gocinema-hdm-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Local HDM bridge for GoCinema box office (TCP → HTTP)",
  "engines": {
    "node": ">=20.13.1"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
